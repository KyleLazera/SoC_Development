#include "xadc.h"

void xadc_init(xadc_handle_t* self, uint32_t core_base_addr)
{
	self->base_addr = core_base_addr;
}

uint16_t read_raw(xadc_handle_t* self, int n)
{
	uint16_t rd_data;
	rd_data = (uint16_t)io_read();
	return rd_data;
}
