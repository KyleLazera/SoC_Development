#ifndef _XADC_H
#define _XADC_H

#include "fpro_init.h"

typedef enum{
	ADC_0_REG = 0,
	TMP_REG = 4,
	VCC_REG = 5
}XADC_REG;

typedef struct{
	uint32_t base_addr;
}xadc_handle_t;

/**
 * @brief Constructor to initialize the base address of the xadc core
 */
void xadc_init(xadc_handle_t* self, uint32_t core_base_addr);

/**
 * @brief Read raw data from the ADC reg
 */
uint16_t read_raw(int n);

/**
 * @brief Read the ADC input to the XADC ports
 */
double read_adc_in(int n);

#endif	//_XADC_H
